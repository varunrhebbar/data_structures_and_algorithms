#include<bits/stdc++.h>
using namespace std;
void decimal_number()
{
	if (decimal_number == 0)
	return ;
	else
	return(decimal_number % 2 + 10 *find(decimal_number/2));
		
}
int main()
{
	int n=8;
	cout<<find(decimal_number);
	return 0;
}

#include<iostream>
#include<string>
#include<bits/stdc++.h>
using namespace std;

void firstrepeating(string s)
{
	sort(s.begin(),s.end());
	for(int i=0;i<=s.length();i++)
		{
			if(s[i]==s[i+1]);
			cout<<s[i];
	return false;
		}			
}
	
int main()
{
	string s="varan";
	if (firstrepeating)
	cout<<
	return 0;
}

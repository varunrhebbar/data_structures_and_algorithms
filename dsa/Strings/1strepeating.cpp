#include<iostream>
#include<string>
#include<bits/stdc++.h>
using namespace std;

bool firstrepeating(string s)
{
	sort(s.begin(),s.end());
	for(int i=0;i<=s.length();i++)
		{
		if(s[i]=s[i+1]);
		cout<<s[i];
	cout<<"No repeating characters";
		}	
}
	
int main()
{
	string s="varan";
	 (firstrepeating);
	return 0;
}
